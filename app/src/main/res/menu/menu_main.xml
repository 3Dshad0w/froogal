<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/action_changePassword" android:title="Change Password"
        android:orderInCategory="100" app:showAsAction="never" />
    <item android:id="@+id/action_logout" android:title="Logout"
        android:orderInCategory="101" app:showAsAction="never" />

    <item android:id="@+id/search"
        android:title="Search"
        android:icon="@drawable/ic_action_search"
        app:showAsAction="ifRoom"
        android:visible="true"
        />

    <item android:id="@+id/maplist"
        android:icon="@drawable/ic_action_view_as_list"
        android:title="maplist"
        app:showAsAction="always"
        />


</menu>
